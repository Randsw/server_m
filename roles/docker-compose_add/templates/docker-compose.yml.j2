# This file is auto-generated by the Mailu configuration wizard.
# Please read the documentation before attempting any change.
# Generated for compose flavor

version: '3'

services:

# Config data provide by ansible role 
  dns:
    image: sameersbn/bind:latest
    restart: unless-stopped
    container_name: bind
    volumes:
     - ./bind:/data/bind
    ports:
      - "{{ ansible_default_ipv4.address }}:53:53/udp"
      - "{{ ansible_default_ipv4.address }}:53:53/tcp"
    environment:
      WEBMIN_ENABLED: "false"
    networks:
      - default

  {# # External dependencies
  redis:
    image: redis:alpine
    restart: always
    volumes:
      - "/home/{{ ansible_user }}/mailu/redis:/data"
    
  # Core services
  front:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}nginx:${MAILU_VERSION:-1.7}
    restart: always
    env_file: {{ docker_compose_env }}
    logging:
      driver: json-file
    ports:
      - "{{ ansible_default_ipv4.address }}:80:80"
#      - "::1:80:80"
      - "{{ ansible_default_ipv4.address }}:443:443"
#      - "::1:443:443"
      - "{{ ansible_default_ipv4.address }}:25:25"
#      - "::1:25:25"
      - "{{ ansible_default_ipv4.address }}:465:465"
#      - "::1:465:465"
      - "{{ ansible_default_ipv4.address }}:587:587"
#      - "::1:587:587"
      - "{{ ansible_default_ipv4.address }}:110:110"
#      - "::1:110:110"
      - "{{ ansible_default_ipv4.address }}:995:995"
#      - "::1:995:995"
      - "{{ ansible_default_ipv4.address }}:143:143"
#      - "::1:143:143"
      - "{{ ansible_default_ipv4.address }}:993:993"
#      - "::1:993:993"
    volumes:
      - "/home/{{ ansible_user }}/mailu/certs:/certs"
      - "/home/{{ ansible_user }}/mailu/overrides/nginx:/overrides"


  admin:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}admin:${MAILU_VERSION:-1.7}
    restart: always
    env_file: {{ docker_compose_env }}
    volumes:
      - "/home/{{ ansible_user }}/mailu/data:/data"
      - "/home/{{ ansible_user }}/mailu/dkim:/dkim"
    depends_on:
      - redis

  imap:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}dovecot:${MAILU_VERSION:-1.7}
    restart: always
    env_file: {{ docker_compose_env }}
    volumes:
      - "/home/{{ ansible_user }}/mailu/mail:/mail"
      - "/home/{{ ansible_user }}/mailu/overrides:/overrides"
    depends_on:
      - front

  smtp:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}postfix:${MAILU_VERSION:-1.7}
    restart: always
    env_file: {{ docker_compose_env }}
    volumes:
      - "/home/{{ ansible_user }}/mailu/overrides:/overrides"
    depends_on:
      - front

  antispam:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}rspamd:${MAILU_VERSION:-1.7}
    restart: always
    env_file: {{ docker_compose_env }}
    volumes:
      - "/home/{{ ansible_user }}/mailu/filter:/var/lib/rspamd"
      - "/home/{{ ansible_user }}/mailu/dkim:/dkim"
      - "/home/{{ ansible_user }}/mailu/overrides/rspamd:/etc/rspamd/override.d"
    depends_on:
      - front

  # Optional services
  antivirus:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}clamav:${MAILU_VERSION:-1.7}
    restart: always
    env_file: {{ docker_compose_env }}
    volumes:
      - "/home/{{ ansible_user }}/mailu/filter:/data"

  webdav:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}radicale:${MAILU_VERSION:-1.7}
    restart: always
    env_file: {{ docker_compose_env }}
    volumes:
      - "/home/{{ ansible_user }}/mailu/dav:/data"

  fetchmail:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}fetchmail:${MAILU_VERSION:-1.7}
    restart: always
    env_file: {{ docker_compose_env }}

  # Webmail
  webmail:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}roundcube:${MAILU_VERSION:-1.7}
    restart: always
    env_file: {{ docker_compose_env }}
    volumes:
      - "/home/{{ ansible_user }}/mailu/webmail:/data"
    depends_on:
      - imap

  database:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}postgresql:${MAILU_VERSION:-1.7}
    restart: always
    env_file: {{ docker_compose_env }}
    volumes:
      - "/home/{{ ansible_user }}/mailu/data/psql_db:/data"
      - "/home/{{ ansible_user }}/mailu/data/psql_backup:/backup" #}

networks:
  default:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: {{ docker_compose_network }}
